# DINO extra dependencies (GroundingDINO + StableDINO helpers).
#
# Install from repo root (recommended):
#   python -m pip install -e DamageDetector
#   python -m pip install -r DamageDetector/object_detection/requirements_dino.txt
#
# Detectron2 is intentionally NOT installed here because pip source builds often
# fail on Colab/Python 3.12/CUDA-mismatched environments. Install a matching
# Detectron2 wheel separately only when using StableDINO train/eval.
# Example for supported Torch/CUDA versions:
#   python -m pip install 'detectron2' -f https://dl.fbaipublicfiles.com/detectron2/wheels/cu121/torch2.1/index.html

# Common detection utilities.
pycocotools>=2.0.7
fvcore>=0.1.5.post20221221,<0.1.6
iopath>=0.1.7,<0.1.10
omegaconf>=2.3.0
hydra-core>=1.3.2
yacs>=0.1.8
termcolor>=2.4.0
tabulate>=0.9.0
cloudpickle>=3.0.0

# StableDINO project code expects detrex.
detrex @ git+https://github.com/IDEA-Research/detrex.git
