# Object detection extra dependencies (YOLO + DINO helpers).
#
# Install from repo root (recommended):
#   python -m pip install -e DamageDetector
#   python -m pip install -r DamageDetector/object_detection/requirements.txt
#
# Detectron2 is optional and environment-specific. Install it separately only
# when running StableDINO training/evaluation. See requirements_dino.txt.

-r requirements_yolo.txt
-r requirements_dino.txt
