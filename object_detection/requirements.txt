# Object detection extra dependencies (YOLO + StableDINO).
#
# Install from repo root (recommended):
#   DamageDetector/.venv/bin/python -m pip install -e DamageDetector
#   DamageDetector/.venv/bin/python -m pip install -r DamageDetector/object_detection/requirements.txt
#
# Or install individually:
#   DamageDetector/.venv/bin/python -m pip install -r DamageDetector/object_detection/requirements_yolo.txt
#   DamageDetector/.venv/bin/python -m pip install -r DamageDetector/object_detection/requirements_dino.txt

-r requirements_yolo.txt
-r requirements_dino.txt
