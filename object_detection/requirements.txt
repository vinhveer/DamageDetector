# Object detection extra dependencies (YOLO + StableDINO).
#
# Install from repo root (recommended):
#   DamageDetector/.venv/bin/python -m pip install -e DamageDetector
#   DamageDetector/.venv/bin/python -m pip install -r DamageDetector/object_detection/requirements.txt

# YOLO (Ultralytics)
ultralytics>=8.3.0

# Common detection utilities
pycocotools>=2.0.7
fvcore>=0.1.5.post20221221
iopath>=0.1.10
omegaconf>=2.3.0
hydra-core>=1.3.2
yacs>=0.1.8
termcolor>=2.4.0
tabulate>=0.9.0
cloudpickle>=3.0.0

# StableDINO depends on Detectron2.
# Note: Detectron2 is easiest on Linux (CUDA or CPU wheels). On macOS you may need to build from source.
detectron2 @ git+https://github.com/facebookresearch/detectron2.git ; platform_system == "Linux"

# StableDINO project code expects detrex.
detrex @ git+https://github.com/IDEA-Research/detrex.git
